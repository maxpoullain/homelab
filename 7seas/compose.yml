name: 7seas

services:
  wireguard:
    container_name: wireguard
    image: thrnz/docker-wireguard-pia:latest
    restart: unless-stopped
    cpus: 0.5
    mem_limit: 512m
    healthcheck:
      test: ping -c 1 www.google.com || exit 1
      start_period: 12s
      interval: 30s
      timeout: 10s
      retries: 3
    security_opt:
      - no-new-privileges:true
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv6.conf.default.disable_ipv6=1
      - net.ipv6.conf.all.disable_ipv6=1
      - net.ipv6.conf.lo.disable_ipv6=1
    networks:
      - traefik
      - 7seas
    volumes:
      - ./pia:/pia
      - .env/:/secrets.env
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    env_file:
      - ./pia/.env
    environment:
      - PUID=3000
      - PGID=3002
      - TZ=Europe/Paris
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.seedbox.entrypoints=https"
      - "traefik.http.routers.seedbox.rule=Host(`seedbox.corsair.tf`)"
      - "traefik.http.routers.seedbox.middlewares=security@file,private@file,auth@docker"
      - "traefik.http.routers.seedbox.service=seedbox"
      - "traefik.http.services.seedbox.loadbalancer.server.port=9091"

  transmission:
    container_name: transmission
    image: linuxserver/transmission:latest
    restart: unless-stopped
    cpus: 4
    mem_limit: 2048m
    security_opt:
      - no-new-privileges:true
    network_mode: "service:wireguard"
    depends_on:
      wireguard:
        condition: service_healthy
        restart: true
    volumes:
      - "./transmission:/config"
      - "./transmission/watch:/watch"
      - "/mnt/fast/downloads/seedbox:/downloads"
      - "/mnt/tank/media/downloads:/tank/downloads"
      - "/mnt/tank/media/tv/:/tank/tv"
      - "/mnt/tank/media/movies/:/tank/movies"
      - "/mnt/tank/media/books/raw:/tank/books"
    env_file:
      - .env
    environment:
      - PUID=3000
      - PGID=3002
      - TZ=Europe/Paris
      - TRANSMISSION_WEB_UI=flood-for-transmission

  flaresolverr:
    container_name: flaresolverr
    image: ghcr.io/flaresolverr/flaresolverr:latest #21hsmw/flaresolverr:nodriver #ghcr.io/flaresolverr/flaresolverr:pr-1282
    restart: unless-stopped
    cpus: 4
    mem_limit: 1024m
    security_opt:
      - no-new-privileges:true
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
    networks:
      - 7seas
    environment:
      - PGID=3002
      - PUID=3000
      - TZ=Europe/Paris
      - LOG_LEVEL=info
      - LANG=fr_FR
      - BROWSER_TIMEOUT=240
      - NO_SANDBOX=True

  prowlarr:
    container_name: prowlarr
    image: lscr.io/linuxserver/prowlarr:latest
    restart: unless-stopped
    cpus: 0.5
    mem_limit: 512m
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik
      - 7seas
    depends_on:
      flaresolverr:
        condition: service_started
    volumes:
      - "./prowlarr:/config"
    environment:
      - PUID=3000
      - PGID=3002
      - TZ=Europe/Paris
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      # Main router for / (with auth)
      - "traefik.http.routers.prowlarr.entrypoints=https"
      - "traefik.http.routers.prowlarr.rule=Host(`prowlarr.corsair.tf`)"
      - "traefik.http.routers.prowlarr.middlewares=security@file,private@file,auth@docker"
      - "traefik.http.routers.prowlarr.service=prowlarr"
      # API router for /api (without auth)
      - "traefik.http.routers.prowlarr-api.entrypoints=https"
      - "traefik.http.routers.prowlarr-api.rule=Host(`prowlarr.corsair.tf`) && PathPrefix(`/api`)"
      - "traefik.http.routers.prowlarr-api.middlewares=security@file,private@file"
      - "traefik.http.routers.prowlarr-api.service=prowlarr"
      - "traefik.http.routers.prowlarr-api.priority=100"
      # Service definition
      - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"

  ygege:
    image: uwucode/ygege:latest
    container_name: ygege
    restart: unless-stopped
    networks:
      - 7seas
    env_file:
      - ygg.env
    healthcheck:
      test: ["CMD-SHELL", "curl --fail http://localhost:8715/health || exit 1"]
      interval: 1m30s
      timeout: 20s
      retries: 3
      start_period: 10s

  sonarr:
    container_name: sonarr
    image: linuxserver/sonarr:latest
    restart: unless-stopped
    cpus: 1
    mem_limit: 1024m
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik
    depends_on:
      - prowlarr
    volumes:
      - "./sonarr:/config"
      - "/mnt/tank/media/tv/:/tv"
      - "/mnt/fast/downloads/seedbox:/downloads"
    environment:
      - PUID=3000
      - PGID=3002
      - TZ=Europe/Paris
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      # Main router for / (with auth)
      - "traefik.http.routers.shows.entrypoints=https"
      - "traefik.http.routers.shows.rule=Host(`shows.corsair.tf`)"
      - "traefik.http.routers.shows.middlewares=security@file,private@file,auth@docker"
      - "traefik.http.routers.shows.service=shows"
      # API router for /api (without auth)
      - "traefik.http.routers.shows-api.entrypoints=https"
      - "traefik.http.routers.shows-api.rule=Host(`shows.corsair.tf`) && PathPrefix(`/api`)"
      - "traefik.http.routers.shows-api.middlewares=security@file,private@file"
      - "traefik.http.routers.shows-api.service=shows"
      - "traefik.http.routers.shows-api.priority=100"
      # Service definition
      - "traefik.http.services.shows.loadbalancer.server.port=8989"

  radarr:
    container_name: radarr
    image: linuxserver/radarr:latest
    restart: unless-stopped
    cpus: 1
    mem_limit: 1024m
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik
    depends_on:
      - prowlarr
    volumes:
      - "./radarr:/config"
      - "/mnt/tank/media/movies/:/movies"
      - "/mnt/fast/downloads/seedbox:/downloads"
    environment:
      - PUID=3000
      - PGID=3002
      - TZ=Europe/Paris
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      # Main router for / (with auth)
      - "traefik.http.routers.movies.entrypoints=https"
      - "traefik.http.routers.movies.rule=Host(`movies.corsair.tf`)"
      - "traefik.http.routers.movies.middlewares=security@file,private@file,auth@docker"
      - "traefik.http.routers.movies.service=movies"
      # API router for /api (without auth)
      - "traefik.http.routers.movies-api.entrypoints=https"
      - "traefik.http.routers.movies-api.rule=Host(`movies.corsair.tf`) && PathPrefix(`/api`)"
      - "traefik.http.routers.movies-api.middlewares=security@file,private@file"
      - "traefik.http.routers.movies-api.service=movies"
      - "traefik.http.routers.movies-api.priority=100"
      # Service definition
      - "traefik.http.services.movies.loadbalancer.server.port=7878"

  readarr:
    container_name: readarr
    image: lscr.io/linuxserver/readarr:0.4.18-develop
    restart: unless-stopped
    cpus: 0.5
    mem_limit: 512m
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik
    depends_on:
      - prowlarr
    volumes:
      - "./readarr:/config"
      - "/mnt/tank/media/books/raw:/books"
      - "/mnt/fast/downloads/seedbox:/downloads"
    environment:
      - PUID=3000
      - PGID=3002
      - TZ=Europe/Paris
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      # Main router for / (with auth)
      - "traefik.http.routers.readarr.entrypoints=https"
      - "traefik.http.routers.readarr.rule=Host(`books.corsair.tf`)"
      - "traefik.http.routers.readarr.middlewares=security@file,private@file,auth@docker"
      - "traefik.http.routers.readarr.service=readarr"
      # API router for /api (without auth)
      - "traefik.http.routers.readarr-api.entrypoints=https"
      - "traefik.http.routers.readarr-api.rule=Host(`books.corsair.tf`) && PathPrefix(`/api`)"
      - "traefik.http.routers.readarr-api.middlewares=security@file,private@file"
      - "traefik.http.routers.readarr-api.service=readarr"
      - "traefik.http.routers.readarr-api.priority=100"
      # Service definition
      - "traefik.http.services.readarr.loadbalancer.server.port=8787"

networks:
  traefik:
    external: true
  7seas:
    external: true
